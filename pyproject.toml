[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"



[project]
name = "lix"
version = "0.1.2"
description = "LIX library for Lowell Instruments"
dependencies = [
    'gsw',
    'numpy~=2.0',
    'python-dateutil'
]